#include <iostream>
#include <vector>
#include <list>
#include <stack>
#include <map>
#include <set>
using namespace std;
int main()
{
    // list<int>l;
    // l.push_back(1);
    // l.push_back(2);
    // l.push_front(3);

    // for(int val:l){
    //     cout<<val<<" ";
    // }
    // cout<<endl;
    // pair<int,pair<char,int>>p={5,{'c',7}};
    // cout<<p.first<<endl;
    // cout<<p.second.first<<endl;
    // cout<<p.second.second;

    // stack<int>s;
    // s.push(1);
    // s.push(2);
    // s.push(3);
    // while(!s.empty()){
    //     cout<<s.top()<<" ";
    //     s.pop();
    // }
    // cout<<endl;

    // map<string,int>m;
    // m["tv"]=100;
    // m["remote"]=50;
    // m["cover"]=20;

    // m.emplace("camera",25);

    // for(auto p:m){
    //     cout<<p.first<<" "<<p.second<<endl;
    // }
    // cout<<"count = "<<m.count("tv");

    // multimap<string,int>m;

    // m.emplace("camera",25);
    // m.emplace("camera",25);
    // m.emplace("camera",25);

    // for(auto p:m){
    //     cout<<p.first<<" "<<p.second<<endl;
    // }
    // cout<<"count = "<<m.count("tv");

    // set<int>s;
    // s.insert(1);
    // s.insert(2);
    // s.insert(3);
    // s.insert(4);

    // for(auto val:s){
    //     cout<<val<<" ";
    // }cout<<endl;

    // vector<int>vec={3,4,1,2,7};
    // sort(vec.begin(),vec.end());
    // for(int val:vec){
    //     cout<<val<<" ";
    // }cout<<endl;

    return 0;
}